const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {
			fontFamily: {
				Taviraj: ['"Taviraj"', 'serif'],
				Kanit: ['"Kanit"', 'sans-serif']
			},
		}
	},

	plugins: []
};

module.exports = config;
